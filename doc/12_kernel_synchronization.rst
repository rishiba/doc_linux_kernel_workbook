######################
kernel_synchronization
######################

The right way to add nodes to the linked list is to add it using some locks. We will see the locks now.

.. todo:: Write a section on kernel synchronization.
